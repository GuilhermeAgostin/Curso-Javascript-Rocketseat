// Operador Condicional (Ternário)

// Dependendo da condição, nós receberemos valores
//diferentes

// Exemplos
// Café da manhã top

let pao = true
let queijo = false

const niceBreakfast = pao && queijo ? 'Café top' : 'Café ruim'

console.log(niceBreakfast)