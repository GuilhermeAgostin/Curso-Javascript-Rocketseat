let one = 1 
let two = 2

// === estritamente igual a
console.log( one === "1") // verifica se o valor da variavel é igual  a comparacao e tambem compara o tipo se for o mesmo retorna true
console.log( one === 1 )

// !== estritamente diferente de // para fazer o estritamente diferente eu uso as keys -> ! + == que fica !==
 console.log( two !== "2")
 console.log( two !== 2 )